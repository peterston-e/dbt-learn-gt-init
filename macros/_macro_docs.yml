macros:
  - name: macro_docs
    description: "A macro to generate documentation for other macros."
    arguments:
      - name: column_name
        description: "The name of the column containing the amount in cents."
        type: string
      - name: decimals
        description: "The number of decimal places to format the amount (default is 2)."
        type: string
